# 技术方案待确认问题清单

> 生成时间：2026-06-20  
> 目的：在写《技术方案文档》前，先确认关键技术细节，避免瞎编

---

## 一、云开发配置

| # | 问题 | 为什么需要确认 |
|---|------|----------------|
| 1 | **云开发是否已开通？** 小程序后台→云开发，是否已开通？ | 决定后续云函数、云存储的配置方式 |
| 2 | **云环境ID是什么？** 开通后会有一个环境ID（如 `cloud1-xxx`） | 写进项目配置 `project.config.json` |
| 3 | **云函数部署方式？** 在开发者工具里右键上传？还是用CLI自动化部署？ | 决定开发流程和CI/CD配置 |

---

## 二、计算引擎封装

| # | 问题 | 为什么需要确认 |
|---|------|----------------|
| 4 | **pension-engine.js 如何封装成云函数？** 是直接 `require` 进来，还是需要改造成云函数格式？ | 决定云函数的代码结构 |
| 5 | **31省配置文件（provinces/*.json）放在哪里？** 云函数里？还是云存储？还是CDN？ | 决定数据加载方式和性能 |
| 6 | **云函数冷启动时间大概多少？** 如果超过3秒，需要优化（预热、缓存） | 决定用户体验 |

---

## 三、数据库设计

| # | 问题 | 为什么需要确认 |
|---|------|----------------|
| 7 | **是否需要数据库？** 用户说"不保存结果"，但可能需要保存：<br>- 小程序配置（如隐私协议版本）<br>- 支付订单记录<br>- 报告生成记录 | 决定是否需要建集合（Collection） |
| 8 | **如果需要数据库，用云开发的数据库吗？** 还是自建MySQL？ | 决定数据库选型 |

---

## 四、微信支付接入

| # | 问题 | 为什么需要确认 |
|---|------|----------------|
| 9 | **微信支付商户号是否已申请？** 需要企业/个体工商户资质 | 决定支付功能能否做 |
| 10 | **支付回调如何处理？** 云函数里写回调接口？还是需要独立服务器？ | 决定支付流程设计 |
| 11 | **虚拟支付资质是否已申请？** 之前查证需要单独申请 | 决定付费功能能否上线 |

---

## 五、报告生成

| # | 问题 | 为什么需要确认 |
|---|------|----------------|
| 12 | **PDF报告如何生成？** 云函数里用 `pdfkit`？还是调用第三方API（如飞书文档API）？ | 决定技术方案和成本 |
| 13 | **报告存储在哪里？** 云存储？还是临时生成直接返回给用户？ | 决定存储方案和过期清理策略 |

---

## 六、前端与云函数交互

| # | 问题 | 为什么需要确认 |
|---|------|----------------|
| 14 | **前端如何调用云函数？** 用 `wx.cloud.callFunction`？还是封装成统一请求层？ | 决定前端代码结构 |
| 15 | **错误处理如何处理？** 云函数报错，前端如何提示用户？ | 决定用户体验设计 |

---

## 下一步

请你逐个回答上面的问题（或者告诉我"不清楚，你来查"）。

我会根据你的回答，生成正式的《技术方案文档》。

**不确定的问题，我会标注 `[待确认]`，不会自己编。**

---

✍️ **你的回答**（可以直接回复，如"1=已开通，2=cloud1-xxx，3=右键上传，4=不清楚..."）：
